name: Test

on:
  push: {}
  schedule:
    - cron: '0 0 1 * *'

jobs:
  success:
    name: Runs
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@master
      - name: Might the force be with you
        uses: ./
